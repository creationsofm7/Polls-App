import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Polls - Create and Vote on Polls",
  description: "A professional polling platform where you can create, share, and vote on polls. Engage with your community through interactive polls and real-time results.",
  keywords: ["polls", "voting", "surveys", "community", "engagement"],
  authors: [{ name: "Polls Platform" }],
  openGraph: {
    title: "Polls - Create and Vote on Polls",
    description: "A professional polling platform where you can create, share, and vote on polls.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <head>
        <link rel="icon" href="/polls-favicon.svg" type="image/svg+xml" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
